# The deck ships from web/. Nothing here is indexed: robots.txt plus an
# X-Robots-Tag header in web/_headers. It is a private client proposal.
[build]
  publish = "web"

[functions]
  # edge functions live in netlify/edge-functions and are picked up automatically
